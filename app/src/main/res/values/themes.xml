<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DesafioPractico1" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.DesafioPractico1" parent="Base.Theme.DesafioPractico1" />
    <style name="ShapeAppearance.Circle" parent="">
        <item name="cornerSize">50%</item>
    </style>
    <!-- Estilo Flat para Inputs -->
    <style name="FlatInputStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">56dp</item>
        <item name="android:background">@android:color/white</item>
        <item name="android:textColor">#263238</item>
        <item name="android:textColorHint">#BDBDBD</item>
        <item name="android:textSize">16sp</item>
    </style>

    <!-- <style name="Theme.DesafioPractico1" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary_flat</item>
        <item name="android:windowBackground">@color/background_flat</item>
        <item name="elevation">0dp</item>
    </style> -->
</resources>